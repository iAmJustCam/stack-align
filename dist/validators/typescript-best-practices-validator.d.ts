import type { ProjectContext, ValidationResult } from '../types';
/**
 * Validates TypeScript best practices that we've learned from fixing errors
 */
export declare function validateTypeScriptBestPractices(context: ProjectContext): Promise<ValidationResult>;
